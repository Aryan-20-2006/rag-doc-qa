services:
  -type: web
    name:rag-doc-qa
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && uvicron main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: GEMINI_API_KEY
        sync: false